# Start local Supabase, then run npm run env:local to generate .env.local.
# Only a publishable/anon key belongs here; no administrative key in the app.
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
